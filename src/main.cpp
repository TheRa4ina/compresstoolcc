#include <iostream>
#include <chrono>
#include <vector>
#include <unordered_map>
#include "src/huffman.h"
using freq_t = uint32_t;

int main(){
    //freq = getFileFreq(path);
    //tree = buildTree(freq);
    //writeFile(tree,path);

    return 0;
}