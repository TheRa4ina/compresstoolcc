#include <iostream>
#include <chrono>
#include <vector>
#include <unordered_map>
#include "src/huffman.h"

int main(){
    //freq = getFileFreq(path);
    //tree = buildTree(freq);
    //writeFile(tree,path);

    return 0;
}